---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 本地 Agent 接入后续：按[本地 Agent 接入规划](/zh-CN/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- Agent Skill 后续：补充网络检索安装、Skill 资源文件管理和可控的本地记忆沉淀。
- 账号体系与后端服务：按仓库根目录「我的规划」下的《账号后端总规划》及 Phase 1-5 执行计划新增 MySQL 8.4 LTS + Go 后端，全站功能登录后使用，AI 渠道全部由服务端配置。第一期（账号邮件与免费赠送风控）、第二期（资源化存储与对象存储）、第三期（双桶点数、充值订单与支付、档位派生与存储配额、模型目录与限时折扣、保留期清理、账号注销、管理后台）与第四期（服务端 AI 转发：报价预扣与失败退还、平台渠道加密与故障转移、异步视频任务、SSE 对话、删除浏览器直连与自定义脚本）已交付。
- 第四期收尾项已处理：429 有按 `Retry-After` 的禁用倒计时；报价再次变化时提示「价格已更新，请再次点击生成」（不自动按新价提交）；视频生成记录带任务句柄，刷新后页面可继续显示进度。
- 第五期（内容审核与人工复核）已交付：提示词与参考图在预扣前预审、产物与上传先隔离后提交、NSFWJS / Detoxify 适配层、管理后台审核页与人工复核补偿、fail mode 启动门禁。
- 第五期部署收尾：`MODERATION_PROVIDER=nsfwjs+detoxify` 需要把两个推理 sidecar（Node 与 Python）部署到 compose 内网，视频审核需要 api 容器内可执行 `ffmpeg`；测试模板当前默认关闭审核，启用 `fake` 仅用于流程联调。
- 公开上线的合规门禁与代码无关，仍待独立完成：算法备案、大模型服务备案、服务条款法务确认与人工复核值守，全部确认后才能开放公开注册与平台模型入口。
- 设计稿接口缺口：已交付社区（发布/浏览/点赞/复刻计数/举报与下架/用户主页）、运营活动（每日签到与邀请返利）、系统设置（公告/开放注册/维护模式/功能开关/边界值）、管理员管理与审计日志、后台收入指标；按决策暂缓的是验证码登录、创作比赛与客服角色/反馈工单（范围记录在仓库根目录《后续功能备忘》与《Admin 后台与多产品规划》）。OAuth 第三方登录、两步验证与登录设备管理当前不做，注册与邮箱验证是唯一的注册路径；文档 CMS 建议直接砍掉。
- 下一轮（已写好规划、未实施）：管理后台迁到 `admin.youc.online` / `sim-admin.youc.online` 并设计多产品共用形态（《Admin 后台与多产品规划》），接入易支付渠道（《易支付接入规划》）。内容审核真实 sidecar（NSFWJS / Detoxify）等待服务器资源与档位方案确认后实施。
- 测试环境部署：只保留测试、正式两套数据库，本地开发共用 `infinite_canvas_test`。编排、镜像/端口隔离、服务器代码目录、密钥与 env、镜像构建、测试域名 Nginx 与证书均已就位，测试站已在 `https://sim-art.youc.online` 运行（app 绑 `127.0.0.1:3100`，MySQL 额外映射 `127.0.0.1:13306`）。仍待实施：真实邮件发送（测试站用 `MAIL_DRIVER=log`）、S3 存储、审核 sidecar、DBeaver 隧道验证、容器重启的完整验收，以及备份。平台渠道（`ai.youc.online`，OpenAI 格式）与四个 `gpt-image-*` 模型已配置，并已跑通一次「报价 → 生成 → 落库 → 取回」的完整图片生成。真实服务器细节见《我的规划/部署方案（测试与正式环境）.md》；正式从已验收 main 版本 tag 部署，先完成备份恢复准备再切域名。
- 本地 Go 与服务器测试 Go 共用测试库：先部署同一个私有测试 S3 桶并验收浏览器预签名读取/CORS，再补迁移、seed、管理员初始化、启动请求收敛和后台任务的执行开关，默认服务器测试 Go 为唯一执行者；明确设置缓存刷新/重启与两端代码的结构兼容。完整模式尚未实现，期间本地只跑 Vite，经 SSH 使用服务器测试 API。实际 deploy env 与本地手册已排除出 Docker 构建上下文；长期运行前补普通媒体清理调度和磁盘监控。
- 正式部署前完成数据库、媒体、env/密钥和版本记录的联合备份及恢复演练；当前 AutoMigrate 无结构回滚，公开上线后落实版本化 migration。备份频率、保留策略与异地位置在实施时确定；如限制管理访问，反代策略同时覆盖 `/admin` 与 `/api/admin/`。
