---
title: 待验收
description: 当前版本已实现、仍需人工验证确认的变更项
---

# 待验收

以下为本版本需要人工验证确认的事项：

## 主题默认亮色与移动端适配（本版本新增，待验收）

- **主题默认值**：主站与 admin 全新访客（无本地偏好）默认**亮色**；顶栏「切换到深色主题」一键切换；切换过的用户偏好保留（不受默认值影响）。登录页/画布/工作台/后台首帧均无暗色闪现（防闪烁脚本同步翻转）。
- **断点约定**（已写入 AGENTS.md 前端规范）：移动 <768（单列、弹层全屏化）/ 平板 768–1023 / 桌面 ≥1024。
- **移动端弹层**：<768 视口下所有左右 Drawer 全屏宽（原 736px 大抽屉不再被裁剪）、上下 Drawer 限高滚动（超高视口内容可滚可达）、Modal 自带钳宽 + 内容滚动——主站与 admin 一套规则自动覆盖。
- **admin Sider**：<992 视口自动折叠为零宽触发块（点按开合），正文全宽；≥992 桌面形态零变化。缩放窗口中途不自动恢复展开为 antd 已知行为，刷新即正确。
- **验收方式**：清站点存储后访问应为亮色；375px 手机宽度过一遍主站主要页面与 admin（无横向溢出、抽屉全屏、Sider 可开合）；桌面 1280 确认布局与此前一致。

## 博客站「深夜排字房」（本版本新增，待验收）

架构与选型见 `docs/content/docs/overview/blog-architecture.mdx` 与 `docs/content/docs/development/blog-plan.mdx`；设计稿 `design/html/blog-draft-*.html` 与后台稿 `admin-blog.html`。本轮 P1–P3、P5 代码全部落地（29 个端点 + Next 前台 + admin 三界面），验收清单如下：

- **部署与线上验收（已完成 2026-09-19，测试环境）**：feature-blog 已推送并在服务器 `/opt/infinite-canvas-test` 切支部署；真实 MySQL 迁移已跑通（`SHOW TABLES LIKE "blog_%"` 五表齐全，迁移前备份在 `/opt/backups/pre-blog-migration/`）；blog-web 运行于服务器 `127.0.0.1:3102`（3101 与 admin 冲突，env 已改 3102）；线上 admin（sim-admin）实测：登录 → 「博客管理」菜单 → 新增栏目入库 → API 建文发布 → blog-web 前台 serve 文章页（goldmark 渲染内容可见）→ 再验证回调 0 失败。部署过程修复三处：authFetch body 对象序列化、Next standalone rewrites 构建期烘焙改运行时代理路由、deploy.sh up 固定重建列表补 blog-web。
- **待用户完成的上线步骤**：① `blog` 域名 DNS A 记录 → certbot 补证书 → `deploy/nginx-blog.youc.online.conf` 落地（模板就绪）；② 主站 `/blog/` 页改导流；③ GSC/必应/百度站长提交；④ admin 编辑器「从零新建」建议人工点一遍完整流程（自动化受 React 19 受控表单合成事件限制，编辑/发布路径已实测通过）；⑤ 验证专用账号已清理，正式使用前确认测试管理员密码可用。
- **迁移注意（硬性）**：`blog_topics` / `blog_posts` / `blog_comments` / `blog_reactions` / `blog_bookmarks` 五张新表，按仓库经验必须在真实 MySQL 上跑一次启动迁移确认（SQLite 单测测不出列长/保留字问题）。SQLite 侧 21 包单测已全绿。
- **Go 内容域**：管理面 17 端点（文章 CRUD/发布/下架/删除、栏目 CRUD、同源预览、评论管理 hide/restore/delete/pin）+ 公共读 3 端点（列表/详情含 `content_html`+`toc`+上下篇/栏目计数）。验收：admin 建栏目 → 建文章（存草稿）→ 发布 → 前台可见 → 下架 → 前台 404 → 删除；slug 重复 409、被引用栏目禁删 409。
- **goldmark 渲染管线**：GFM 表格/任务列表/删除线、脚注、标题锚点目录、Chroma monokai 服务端高亮、`> [!TIP]/[!WARNING]` Callout（自写渲染后重写，goldmark 1.8 无内建 Alert）。验收：在编辑器「同源预览」对照前台实际渲染是否一致。
- **Next 前台（blog/ 目录，端口 3101）**：首页（头条/筛选 chips/混合网格/分页/栏目索引）、文章页（目录/上下篇/标签/转载标注/最后更新）、栏目页（列表+分页）、栏目索引、关于页、404、RSS 全文（/feed.xml）、sitemap/robots、逐页 head + JSON-LD（WebSite/Blog/BlogPosting/BreadcrumbList 缺 Breadcrumb——发布时补）、明暗双主题（跟随系统 + 手动切换 + 防闪）。验收：`cd blog && npm install && npm run dev`（需 Go API 在 8080），逐页走查 + Lighthouse。
- **互动（P5）**：评论（登录、回复、短 Markdown、先发后审命中进隔离）、点赞（文章/评论幂等）、收藏 + 我的收藏页。admin 评论台隐藏/恢复/删除/置顶，冗余计数（like/comment/bookmark_count）随写路径增减。验收：登录发评 → 列表可见 → 隐藏后列表消失为占位 → 恢复；重复点赞计数只加一；删他人评论 404。
- **按需再验证**：api 侧 `BLOG_REVALIDATE_URL`/`BLOG_REVALIDATE_SECRET` 与 blog-web 侧 `REVALIDATE_SECRET` 一致时，发布/下架/删除/评论管理后回调失效缓存；回调失败兜底 300s 过期。验收：改文章标题 → 发布 → 前台文章页秒级更新。
- **管理后台**：侧边栏新增「博客管理」（blog.read/write 权限点，admin 角色默认全有）；文章列表筛选/搜索/分页、编辑器（元信息 + Markdown 双栏 + 同源预览）、栏目管理（引用禁删）、评论管理（状态筛选/展开预览/操作）。验收：无 blog 权限的角色登录后台应看不到该菜单且直输 URL 落无权限页。
- **部署物**：compose 新增 `blog-web` 服务（BLOG_WEB_IMAGE/BLOG_WEB_PORT），nginx 模板 `deploy/nginx-blog.youc.online.conf`（/_next/static 长缓存、/api/v1/blog 同域代理），env 示例补 BLOG_* 变量。上线还差：DNS blog A 记录、certbot 补证书、nginx 落地（P4，需真实环境）。
- **订阅功能按约定暂缓**：设计稿与页面无订阅入口（RSS 全文保留）；后续想清楚形态再评估。

## 用户反馈工单与生成点踩（本版本新增，待验收）

- **support 系统角色**：启动同步后角色表出现「客服」系统角色（绑定反馈工单两个权限点、不可删除）；admin 用户管理可把账号任命为客服，该账号登录后台只见「反馈工单」模块。
- **用户端反馈页**：用户菜单「意见反馈」→ 提交工单（类别：生成效果/功能建议/充值支付/账号问题/其他，正文 2-1000 字）→ 「我的工单」列表按状态筛选 → 详情抽屉可见客服回复并继续对话 → 可关闭工单；在已解决/已关闭工单上回复会自动重新打开。工单提交限流 10 次/小时/用户，超限 429。
- **admin 反馈工单台**：侧边栏「反馈工单」→ 工单列表（状态/类别筛选 + 分页，带提交人）→ 详情抽屉回复、标记已解决/关闭/重新打开；「生成点踩反馈」页签按点赞/点踩筛选，列表带问题标签、补充说明、用户与生成记录。
- **生成结果点赞点踩**：图像/视频工作台结果卡信息行出现赞/踩按钮——点赞直接提交；点踩弹反馈弹窗（预设标签多选 + 补充说明，可全空只提交点踩）；再点已选中的按钮撤销；换选先覆盖旧反馈。历史生成记录（从日志恢复）同样可评；旧记录里没有生成 id 的视频结果不可评。
- **迁移注意**：`feedback_tickets` / `feedback_ticket_replies` / `generation_feedbacks` 三张新表，按仓库经验需在真实 MySQL 上跑一次启动迁移确认。

## 浏览器自动化首轮验收（本版本新增，首轮已完成）

本地起真实 server（SQLite）+ vite dev 主站与 admin + 假 OpenAI 上游 + fake 审核通道，按 `records/tests/infinite-canvas-acceptance/README.md` 用浏览器逐流程执行了一轮：

- **通过**：未登录重定向与登录/登出；画布列表与示例画布渲染；工作台按模型约束渲染并显示预估点数；图片生成全链路（报价 → 假上游参数逐项正确 → 媒体落盘 → 扣点与报价一致）；结果卡下载/加参考/存为素材；素材列表与发布；社区卡片与详情的「AI 生成」标签；复刻完整链路（详情跳工作台自动加载原作为参考图 → URL 参数清除 → 发布弹窗「复刻自《原作》」→ 透传 sourceWorkId → 原作 remix +1）；复刻来源可取消；admin 非管理员说明页；用户列表 0 用量显示「0 B / 50 MB」；审核记录 stage=upload/artifact 正确落库；OIDC 承接页登录态自动回跳（redirect 带 code 与 state）与未登录落 /login；authorize 第 21 次 429 带 Retry-After；跨端口共享 refresh cookie 的轮换接管行为正常。
- **发现并已修复两处缺陷**（详见 CHANGELOG）：admin 面接口被共享请求层误加 `/api/v1` 前缀导致后台整站不可用（服务端测试全绿未能发现，属前端拼接回归）；限流器 Retry-After 写成纳秒值。
- **本轮未覆盖（环境限制，留待 sim 环境）**：真实 AI 渠道、真实邮件投递、支付渠道回调、MySQL 真库迁移（本机 docker 异常未起成）、水印链路（需 ffmpeg+字体，WATERMARK 关闭）、视频生成。

## 全站接口规范（本版本新增，待验收）

- **OpenAPI 规范落库**：`docs/api/openapi.yaml`（OpenAPI 3.0.3，由服务端各域路由代码逐 handler 提取生成），覆盖 `/api/v1` 用户面与 `/api/admin` 管理面共 100 个路径、131 个操作，含鉴权方式、统一错误信封、分页口径与字段级 schema。验收方式：在 Apifox「项目设置 → 导入数据 → OpenAPI/Swagger」导入该文件，核对接口目录与字段展示是否完整可用；测试环境已选为默认 server（正式环境 server 预留 youc.online）。
- 校验基线：YAML 语法/重复键/`$ref` 引用脚本校验通过；登录、公开设置、jwks 等抽样端点已对测试环境实测（见「server 结构重构与 API 版本前缀」节的 HTTP 冒烟记录）。

## 功能收尾批（本版本新增，待验收）

- **admin 存储配额 0 值**：admin 用户列表/详情里已用量为 0 时显示「0 B / 1.0 GB」而不是「/ 1.0 GB」；web 端各处 0 字节场景同样显示「0 B」。
- **下载提示统一与防重**：素材页/图片工作台/视频工作台三处下载按钮在请求中显示 loading 且连点不重复申请；失败提示统一为「错误码文案 → 后端 message → 通用兜底」三级口径（素材页不再用固定文案）。
- **OIDC 限流**：`/api/v1/oidc/authorize` 与 `/api/v1/oidc/token` 各挂 IP 级限流 20 次/10 分钟（与登录端点同口径），超限 429 `RATE_LIMITED` + `Retry-After`；浏览器直跳与第三方 token 交换都在范围内。
- **对账告警邮件（D9）**：夜间存储对账发现漂移并自动收敛后，向全部 admin 角色账号邮箱发一封告警邮件（`MAIL_DRIVER=log` 时在 api 日志可见）；无漂移不发信，发送失败不影响对账。
- **隔离件人工放行水印归属（评审 E-4）**：上传被拒的隔离件（审核记录 stage=upload）人工释放后为原始字节，永不烧水印；生成产物被拒的隔离件（stage=artifact）释放时按物主档位处理——免费/日落档烧录水印并留存干净原件（升级后可下发干净版），付费档直接落原始字节；水印失败时释放失败、隔离原件保留可重试，绝不落无水印字节。
- **社区 AIGC 标签**：工作台「存为素材」的素材带 `isAIGC=true`（手动上传为 false），发布作品后社区卡片与详情弹窗显示「AI 生成」蓝色标签。注意 `assets.is_aigc` 为新增列（默认 false），按仓库经验需在真实 MySQL 上跑一次启动迁移确认 AutoMigrate 无异常。
- **社区复刻完整链路**：图片作品详情点「以此作品为素材创作」→ 跳生图工作台并自动把原作加载为参考图（提示「已加载原作《标题》为参考图」）；之后把生成结果存为素材并发布时，发布弹窗顶部显示「复刻自《原作》」标签且随发布透传 `sourceWorkId`（原作 remix 计数 +1），标签可手动取消。视频作品暂无参考视频槽位，保留复制链接的旧闭环。
- **画布 WebP 缩略图**：画布图片节点、批量展开子图、节点参考栏、左侧元素列表在未放大时渲染 768px WebP 本地预览（IndexedDB `image_previews`），放大超过预览像素后自动回退原图；上传图片时顺手生成预览，历史画布图片首次打开时后台补生成；删除素材后对应预览一并清理。平移大图应明显更顺滑。
- **OIDC 承接页**：浏览器直跳 `/api/v1/oidc/authorize?...` 的第三方产品若把回调入口改为前端 `/oauth/authorize`（携带同样参数），已登录用户会看到「正在为你授权并跳转…」并自动回跳第三方（redirect 带 code 与 state）；未登录先到登录页，登录后回本页继续；授权失败（如 redirect_uri 未注册）显示错误与重试按钮。

## server 结构重构与 API 版本前缀（本版本新增，待验收）

- **API 路径变更**：用户面接口从 `/api/...` 变为 `/api/v1/...`（如 `/api/v1/auth/login`、`/api/v1/canvases`、`/api/v1/media/{key}`）；管理面保持 `/api/admin/...` 不变。web 与 admin 前端统一 client、媒体直链、AI 流式请求已同步，浏览器侧应无感知；如有外部脚本直调旧路径需自行更新。
- **回归验证点**：登录/注册/刷新、画布 CRUD、素材、AI 生成（报价/图片/视频/音频/对话流式）、媒体上传与下发（`ic_media` cookie）、点数余额/充值档位/下单购买会员、订单与支付回调、个人中心导出，全链路确认 `/api/v1` 前缀无遗漏。服务端 20 个测试包全绿、web/admin typecheck 与 import 边界检查通过，但真库与浏览器链路仍需人工过一遍。
- **HTTP 层冒烟（2026-09-19 已过）**：测试环境实测旧路径 `/api/auth/*` 404、`/api/v1/settings/public` 与 `/api/v1/oidc/jwks.json` 200、登录 200 且 cookie 口径正确（`ic_refresh` Path=/api、`ic_media` Path=/api/v1/media）、me/canvases/assets/models/credits/plans/orders/generations/community/activity 全部 200、查他人视频任务 404、非管理员访问 `/api/admin/*` 403、未登录媒体 401。浏览器逐页回归仍待做。
- **纯结构重构**：`server/internal/handler` 已拆分为 account/canvas/ai/billing/admin 五域包 + httpx/testutil 共享件，无行为变化；落地规则已写入 AGENTS.md「Server 结构规范」。
- **部署注意**：仓库内 `nginx.conf` 的 SSE/媒体 location 已同步为 `/api/v1/ai/`、`/api/v1/media/`；sim 环境主站域的 `/api` 反代在仓库外的 nginx 配置（`sub2api-http.conf`）里，其中若有按 `/api/ai/` 前缀做的 SSE 特殊处理（proxy_buffering / gzip 豁免），需同步改为 `/api/v1/ai/`，否则对话流式输出会被缓冲。

## 主站首页设计稿（本版本新增，待验收）

- **聚合主站首页草图**：打开 `design/html/site-home.html`，在桌面与手机宽度下核对产品、无限画布、图像/视频工作台、Workers、博客、社区与文档入口的聚合信息架构，以及锚点导航、明暗切换、图像/视频/Worker 模式切换、预览播放态与生成按钮反馈；纯静态设计稿，不连任何接口。
- **首页第二版视觉探索**：打开 `design/html/site-home-v2-index.html` 对比四个保留变体（`site-home-v2-cinema.html`、`site-home-v2-editorial.html`、`site-home-v2-aurora.html`、`site-home-v2-playground.html`）在桌面与手机宽度下的效果：Cinema 全屏作品轮巡、Editorial 精选轮换与 Playground 的可交互工作模式切换；均为静态设计稿，使用公开演示素材、不连接口。`design/html/` 现共 39 个静态稿，总览入口 `index.html` 已接入上述新页。

## 上游同步：画布性能与交互（合入 upstream v0.19.0，待验收）

- **拖拽合批**：拖动单个节点、多选或整组时应保持跟手，松手后的落点应与拖拽预览一致；拖节点到组上方时组高亮即时出现/消失，松手加入组、拖出移除，拖组节点自身不触发任何组高亮。
- **连线视口裁剪**：缩放或平移到只露出一部分画布时，所有穿过视口的连线都应正常渲染（含两端都在屏幕外的连线），连线点选与右键菜单可用，新建连线时的虚线预览仍跟随光标。
- **按钮悬停显示**：画布图片/文本上层的下载与「设为主文本」按钮改为鼠标移入节点后才显示，移出隐藏，不遮挡画面内容；悬停期间点击功能正常。
- 本次同步未合入的 upstream 改动：WebP 本地缩略图预览（依赖我方已删除的本地图片存储，需移植到服务端媒体架构后另行引入）、生图请求 10 分钟超时（服务端已有超时控制）。

## 平台账号与会员体系（本版本新增，已部署测试环境并完成首轮自动化验收）

以下各项已在 sim 测试环境完成一轮浏览器自动化验收（发放→生效→作废→回落全链路），正式验收以你的人工复核为准：

- **登录与回归**：注册/登录/刷新/登出行为与切换前一致；`ic_refresh` cookie Path 已从 `/api/auth` 扩为 `/api`（仍 host-only + Lax + HttpOnly）；F5 硬刷新保持登录。
- **会员订阅链路**：admin「会员管理」发放会员 → 主站个人中心「当前档位」立即变付费、存储配额同步升档（免费 50MB / 付费 1GB / 日落 200MB）；「作废」后立即失去付费身份并进入 60 天日落宽限（宽限至 = 作废时刻 + 60 天），配额同步回落。
- **点数共享余额**：充值/消费/退款先扣赠送再扣购入；流水带 product 维度；`/api/orders` 请求体新增可选 `planId`（与 `packageId` 二选一）购买会员。
- **admin 新模块**：会员管理（订阅列表筛选分页、发放/续期、补偿发放、作废）；users 列表与详情新增「会员档位」「存储配额」列；SSO 接入页（新增客户端 → 一次性密钥展示 → 编辑 → 重置密钥 → 删除）。
- **OIDC**（自动化测试已覆盖，人工按需抽验）：authorize 强制 PKCE（S256）、未注册 redirect_uri 拒绝且不重定向、code 一次性、token 15 分钟 RS256、userinfo 拒绝平台会话令牌。
- **对账兜底**：夜间对账任务（24h 周期）比对三层存储记账并自动收敛，订阅自然到期的配额回写由此兜底；`storage_reconcile_drift` 结构化日志可在 api 容器日志中观察。

已知小瑕疵（不阻塞）：admin users 页「存储配额」列在已用量为 0 时渲染为「/ 1.0 GB」（用量数字留空），后续补一个 0 的展示。

## 媒体水印与干净原件下发（本版本新增，待验收）

**部署前提**：api 容器 PATH 内需有 ffmpeg/ffprobe（ffmpeg 需含 libx264，视频水印重编码用），`WATERMARK_FONT_PATH` 指向一个可读的 CJK 字体文件（建议 Noto Sans SC）；`WATERMARK_ENABLED=true` 且字体缺失会拒绝启动（fail-fast）。`WATERMARK_ENABLED` 默认 false——关闭时生成/展示/下载行为与本功能上线前完全一致，先验这条回归再开 flag。

- **flag=false 回归**：免费账号生成、画布/工作台/素材页展示、三处下载按钮、画布导出，行为与现状一致（无水印、下载直下）。
- **免费链路（flag=true）**：免费账号生成图片与视频，画布节点、图片/视频工作台、素材页展示的均为带平铺「infinite-canvas」水印的版本；素材页/图片页/视频页点下载得到水印文件；webp 源生成的图片展示正常（水印版转 PNG）。
- **格式边界（flag=true）**：上游返回 gif 图片或 webm 视频时，免费账号生成应报失败并退还点数/试用次数（fail-closed，绝不下发未打水印的干净件）；付费账号不受影响。
- **付费链路与升级回溯**：付费账号生成全程无水印；免费期间生成过的素材，账号升级付费后刷新画布/素材页即变为干净版（展示与下载都是）；点数耗尽降级或权益到期后回到水印版。
- **申请下载两步流**：付费账号对免费期生成的素材点下载，网络面板应见先 `POST /api/media/{key}/download` 返回 `/api/media-download/{token}` 与 `expiresAt`（≤300 秒），随后取件的文件为无水印原件，文件名 `ic-{对象id}.{扩展名}`；免费账号点下载拿到的是水印版、无签发请求语义差异。
- **防盗链**：签名 URL 过期后再取应 404；把签名 URL 发给另一个登录账号取件应 404（须同时校验本人媒体登录态）；篡改 token 404；对他人（含社区已发布作品的浏览者）调 `/download` 应 404。
- **社区语义**：免费作者发布的作品，其他用户浏览时为水印版；付费作者发布的作品为干净版（付费权益随作品展示）。
- **清理**：删除素材、注销账号后，服务端存储 `{uid}/orig/` 下的原件对象同步删除（可在服务器媒体目录核对）。
- **限流**：同一账号高频点下载（>60 次/小时）应被限流，展示不受影响。

## 主站静态站骨架（main-site，本版本新增）

- `cd main-site && npm install && npm run build` 成功，`out/` 生成 `index.html`、`works/`、`films/`、`templates/`、`blog/`、`404.html`、`sitemap.xml`、`robots.txt`、`og.png`。
- 静态服务器（如 `python3 -m http.server -d out 4173`）逐页打开：首页 Hero 视频背景、跑马灯滚动、横向轨道滑动正常；断网打开时视频自动降级为渐变底、版式不破。
- 五页顶部导航互跳正确且当前页高亮；每页 title 与 canonical 唯一；页面源码可见 JSON-LD（Person + WebSite）与 OG 标签；`/sitemap.xml`、`/robots.txt` 可访问且域名取自 `site.url`。
- 375px 手机宽度下五页布局不破、无横向溢出。
- 内容更新流程演练：改 `src/data/site.ts`（如新增一张作品）→ `npm run build` → `out/` 生效。
- 上线前替换占位资源（清单见 `main-site/README.md`）：Hero 视频、picsum 占位图、og.png、社媒与下载链接。
- 官网已部署服务器：产物在 `/var/www/youc.online`，nginx 站点 `youc.online` / `www.youc.online`；DNS A 记录（@ 与 www）已指向服务器，证书已签发（2026-12-18 到期，certbot 自动续期），80→443 跳转生效，五页 + sitemap + robots 全路径 HTTPS 200——待浏览器正式访问验收。2026-09-19 已复核五页跟随斜杠 301 后全部 200，剩余浏览器视觉验收。浏览器实测（375px 与桌面）：五页内容渲染完整、图片 0 加载失败；`/works` `/films` `/templates` `/blog` 无横向溢出，首页 375px 溢出约 6px 已修复（根因是跑马灯条带 scale(1.03) 超出视口而 html 层未裁剪，补 `html { overflow-x: clip }`；浏览器 375px 实测 documentElement 溢出为 0）；BILIBILI 与小红书两个页脚外链仍是 `#` 占位。

## 多产品后台与模型能力分区（本版本新增）

**已在测试环境部署并验收通过（最新 `edd7d86`）**：侧边栏两层菜单——「优刻画布」展开含全部模块、「模型」下挂图片模型(5)/视频模型(1)/音频模型(0)/文本模型(0) 子项且数量正确、博客/办公套件带「规划中」进占位页；点击「视频模型」跳 `/admin/models?capability=video` 且筛选预选、表格只剩 1 行；视频模型编辑抽屉只渲染视频分组，老数据与价格矩阵原样保留。修复了模型表列头显示原始 i18n key 的旧缺失（`admin.models.columns` 语言包键）。受限角色的模块过滤由自动化测试覆盖。**待你在常用浏览器复核**：admin 站 F5 硬刷新保持登录（服务端 cookie 旋转已用 curl 验证正常；自动化浏览器里发现整页加载后的会话恢复偶发失败，疑似内嵌浏览器环境的 cookie 存储怪癖，需要在真实浏览器确认）。

原有验收清单（浏览器运行时项）保留如下：

- admin 侧边栏顶部出现产品切换器：当前「优刻画布」显示版本号；选「博客」「办公套件」应落到「未接入」占位页（说明接入条件），不渲染任何管理菜单，点「返回当前产品」回后台。
- 模型页顶部能力筛选（全部/图片/视频/音频/文本）计数正确，切换过滤正常。
- 编辑一个既有图片模型：尺寸/质量/背景/张数/特性分组出现且带原值；编辑视频模型：分辨率/比例/时长/特性分组正确；编辑老模型若有预置分组之外的约束键，应出现在「自定义约束」里且保存后不丢。
- 新建一个视频模型（分辨率+比例+时长）能保存，价格矩阵按所选维度组合生成。
- 受限角色（如只授 `users.read`）登录后台：产品切换器仍可见，但 `GET /admin/meta` 返回的模块清单只含用户模块（后端已按权限过滤，自动化测试覆盖）。

## 测试环境部署与首轮验收结果（2026-09-18）

`sim-art.youc.online` / `sim-admin.youc.online` 已更新到本分支（`295f0ff`），升级前备份在服务器 `/opt/backups/infinite-canvas-test/20260918-141558`（库+媒体+env）。**以下各项已在真环境验收通过**：

- 部署：镜像构建、容器滚动更新、`schema_migrations` 版本行写入，`media_token_version` / `quarantine_bytes` / `used_free_trial` 三个新列就位。
- 主域 `/admin`、`/admin/models` 真 404（nginx 已应用），首页与业务路径正常；sim-admin 带 noindex。
- `/terms`、`/privacy` 未登录可访问，占位标注正确；注册页有「我已阅读并同意」勾选框与条款链接。
- 个人中心「免费体验额度」卡：领取成功（图片 3 次 / 视频 1 次）并显示剩余次数（验收时已把测试环境 `FREE_GRANT_ENABLED` 打开并补齐 campaign/budget/threshold）。
- 社区跨用户媒体：管理员发布作品，测试账号浏览时缩略图正常加载（#31）。
- admin 独立站：非管理员账号落说明页且不登出；独立登录页；管理员登录后台完整；用量分析页（KPI/趋势/分布/排行）正常渲染。
- 维护模式：开启后普通用户写 503 `MAINTENANCE_MODE`、读正常、管理员不受限，关闭后恢复（支付回调豁免由代码路径保证）。
- 强制改密闭环：管理员重置 → 登录置位 → 业务 403 `PASSWORD_CHANGE_REQUIRED` → 改密 → 业务恢复，旧密码失效。
- 数据导出：`GET /api/me/export` 返回 200 与 `youc-export.json` 附件，含 profile/credits/orders/generations/canvases。
- 顺手修复并验收：`/assets` 直开被应用内 nginx 目录重定向打到内部端口 3000 的问题（`nginx.conf` try_files 去掉 `$uri/`）。
- **定时备份（2026-09-19 落地）**：服务器 root crontab 每天04:00 执行 `scripts/backup.sh --yes`（库 + 媒体 + env 三件套 → `/opt/backups/infinite-canvas-test/`，日志 `cron.log`）；实跑验证 dump 完整（36 张表），脚本补 `--no-tablespaces` 解决低权限账号的 PROCESS privilege 报错。验收方式：次日在服务器查看 `/opt/backups/infinite-canvas-test/` 新增目录与 `cron.log`。

**仍待真环境验收**（依赖真实渠道/邮件/sidecar/商户配置）：真实生成扣费链路（自动化测试已覆盖）、视频审核链路（测试环境未开 `MODERATION_ENABLED`）、真实邮件投递（当前 `MAIL_DRIVER=log`）、易支付端到端（未配置商户）。

## 差异清单批量修复（本版本新增，待验收）

- **账号与合规**：生产模板下 `REGISTRATION_ENABLED` 未设置时应默认关闭注册；申请注销后 7 天内仍能登录、刷新并从用户菜单撤销注销，撤销后账号恢复；冷静期内调生成接口应 403 `ACCOUNT_PENDING_DELETION`；重置密码邮件 1 小时内有效、验证邮件 24 小时；同一重置链接并发打开只能用一次；改密/重置密码后旧 `ic_media` 媒体 cookie 立即失效（图裂后自动恢复需重新登录）；`/terms` 与 `/privacy` 可未登录访问，注册不勾选同意无法提交；个人中心「数据导出」下载的 JSON 含资料/余额/订单/生成与画布元数据。
- **免费额度**：领取入口在个人中心，领取后生成显示剩余次数；超 cheapest 参数组合（如 n>1、非最小尺寸）应走点数扣费而非免费；未领取/被拒用户生成按点数计费；并发提交同一账号的免费生成不能超发；生成失败后试用计数应退还；邀请绑定：注册不满 1 小时、邮箱未验证会报具体原因。
- **社区与上传**：用户 B 浏览用户 A 已发布作品的图片/视频应正常显示（未发布/已下架仍 404）；上传伪装成图片的非图片文件应 400「Content-Type 与文件内容不符」；同一用户一天最多发 20 篇作品、同素材同标题当日重复发布 409；源素材删除后作品从公开流消失。
- **审核链路**：开审核（含 fake provider）后视频任务应正常到 succeeded/failed 终态，不再永久 moderating；被拒视频原件保留隔离区、管理端可预览可释放；语音产物有 provider=none 的审核记录且正常可用；隔离区统计字节数与真实对象大小一致；正式环境 `MODERATION_ENABLED=true` + fake 应拒绝启动，其它环境有明确告警。
- **易支付**（配置 `EASYPAY_ENABLED=true` 后）：充值弹窗出现易支付渠道，跳转收银台支付后订单到账且只到账一次；金额被篡改的回调被拒绝；渠道回调返回 `success` 文本；未启用时界面与接口都不出现该渠道。
- **维护模式与并发**：站点设置开启维护模式后，普通用户写接口 503 `MAINTENANCE_MODE`、浏览正常、管理员不受限、支付回调照常；生成接口默认全局并发 8、每用户每分钟 30 次，超限 429 带 Retry-After。
- **admin**：登录响应带 `mustChangePassword` 的账号进后台被强制导向改密页，改密成功后正常进入；admin 社区用户页链接落主站域（`MAIN_SITE_BASE_URL`）；主站域名访问 `/admin` 返回 404（需在宿主机应用新 nginx 模板后验证）；正式环境侧边栏显示红色「正式环境」角标。
- **部署**：按根 `.env.example` 填好后 `docker compose up -d` 一键可起；本地 `go run ./cmd/server` 能读取仓库根 `.env`；`scripts/backup.sh` 备份出「库 + 媒体 + env」三件、`restore.sh` 可复原；启动后 `schema_migrations` 表写入版本行。
- **画布与工作台**：模型目录请求失败后选择器显示「模型加载失败 + 重试」；两个视频同时生成时结果卡各自更新不互相覆盖；删除历史失败有 toast；Firefox 滚轮缩放正常；Agent 写回提示词后节点输入框立即反映新内容；任一页面渲染抛错显示中文错误页（可返回首页/重试）。

- 前端设计稿：`design/html/` 下共 23 个静态 HTML 设计稿（含总览入口 `index.html`、前台 11 页、后台 7 页、组件规范与画布主题对照），全部支持浅色/深色切换与移动端布局；关键交互均已 Mock 化（不调接口、随机成功/失败提示）：工作台生成流程（成功/失败/内容审核拒绝/取消三态）、登录注册表单校验与跳转、后台用户勾选批量赠送/封禁解封、模型上下架与移除、渠道连通测试、审核卡片通过拒绝移除、提示词上下架、活动结束/删除、订单取消、补偿弹窗、素材增删与批量操作、社区点赞计数、模型约束的自定义标签添加（带格式校验与去重）。另新增[设计稿与接口对照](/docs/progress/design-api-coverage)文档，记录设计稿与后端规划的覆盖对照、三处冲突与九项接口缺口；三处冲突已修订，内容审核方案已从阿里云付费服务改为开源方案（图片 NSFWJS、文本 Detoxify），相关规划文档与设计稿已同步。
- 本地代理：配置弹窗应在「渠道」和「偏好设置」之间出现独立的「本地代理」页签，开关默认关闭。打开后应显示 `npx @basketikun/canvas-proxy@latest` 启动命令（端口非默认时自动追加 `--port`）、复制按钮，以及默认 `http://127.0.0.1:23210` 的地址输入框，失焦时自动补全协议并去掉末尾斜杠；「测试连接」应只在命令已启动时成功。开启代理后，拉取模型列表、生图、生视频、生成文本（含流式输出）、生成音频、自定义调用脚本、视频任务轮询，以及生成结果图片和视频的下载都应经代理转发且功能正常，关闭后恢复直连。代理本身应原样转发请求方法、请求头、查询参数和请求体，SSE 响应按块透传不缓冲，`data:` 和 `blob:` 地址不受影响。
- 图像与视频尺寸设置：图像设置应将分辨率（`1k` / `2k` / `4k` / 自动）和宽高比（`1:1`、`2:3`、`3:2`、`4:3`、`3:4`、`16:9`、`9:16`、`21:9`、`9:21`、自动）拆成两项，并由二者计算 W/H。视频设置仍使用 `480p` / `720p` / `1080p`（以及自定义 p 值），比例为 `1:1`、`3:4`、`4:3`、`16:9`、`9:16`、`21:9`、自动，并由清晰度与比例计算 W/H。秒数应为 4–30 的滑杆，右侧显示当前秒数；比例为自动时应禁用 W/H。
- 视频内置请求：未配置调用脚本时，OpenAI 格式应按模板发送 `mode`、`first_frame`/`last_frame` 或 `image[]`、`generate_audio`、`watermark` 以及参考视频/音频，不再使用 `input_reference[]`；Gemini 格式应走 `predictLongRunning`，按模式发送 `image`/`lastFrame` 或 `referenceImages`，不再提示不支持视频。刷新后应能继续查询 Gemini 任务状态。
- 视频生成模式：视频设置应出现「首尾帧」和「全能参考」；1 张图按首帧、2 张图按首尾帧发送专门字段，超过 2 张即使选了首尾帧也按全能参考发送。内置请求和调用脚本的 `params.mode` 均为 `frames` 或 `reference`。
- Gemini 生图：Gemini 格式渠道和 Gemini 生图调用脚本应使用 `generationConfig.responseModalities: ["TEXT", "IMAGE"]`，比例和尺寸放在 `imageConfig.aspectRatio` / `imageConfig.imageSize`，不再发送 `responseFormat`。
- 视频调用脚本：能力为「视频」的模型打开「调用脚本」后应进入全屏分步配置；左侧按「看懂规则 → 让外部 AI 写 → 可用变量」展示，可用变量应包含 `videos`（参考视频 File 数组）和 `audios`（参考音频 File 数组）；「复制写脚本说明」应包含当前能力、可用变量、返回要求和写法约束。插入的模板应是带参数列表的 async function，并把 `params` 拆开；OpenAI 生图模板必须包含 `quality`、`size`、`count`、`background`。OpenAI 视频模板应使用 FormData，带上 `mode`、时长、尺寸、分辨率、配音、水印以及参考图/视频/音频；`mode=frames` 时应使用 `first_frame` / `last_frame`，`mode=reference` 时应使用 `image[]`，完成后轮询并下载 `/content`。把外部 AI 写好的脚本粘贴到右侧并保存后，画布连接参考视频或参考音频再生成时，脚本应能读取这些文件。
- URL 凭据导入：使用新的有效 `baseUrl` 和 `apiKey` 打开应用时，应新增对应渠道且不改动已有渠道；再次导入相同 Base URL（含末尾斜杠或 `/v1` 变体）时应只更新该渠道并保持幂等；只有 apiKey 没有 Base URL、或 Base URL 无效时，应保持所有设置不变。
- 文档站导航与本页目录：手机宽度打开文档侧边栏时，顶部应显示当前文档分类及说明；展开后可在项目介绍、操作手册、开发文档、项目进度、商务合作和赞助支持之间切换，切换后分类标题及下方页面列表应同步更新；桌面端顶部分类栏保持不变，宽度达到 960px 时正文右侧应显示带层级折线的本页标题目录，点击可跳转，滚动时自动高亮当前标题；较窄宽度继续显示顶部折叠目录。
- Canvas Agent 生成流程引用：提示词仅包含已经通过 `referenceNodeIds` 传入的 `@[node:...]` 引用时，应直接把现有文本、图片、视频、音频或组节点连接到生成配置节点，不再创建重复的提示词文本节点；提示词包含普通文字时仍应创建独立的提示词文本节点。
- 视频画面截取：右键有内容的视频节点应显示「截取首帧」「截取尾帧」「截取当前帧」；三项操作应按视频原始比例在右侧生成并选中图片节点，同时建立视频到图片的连线；「截取当前帧」应使用播放器此刻停留的画面，截取过程不得改变原视频的播放位置。
- 文本节点：节点正文右上角不再显示「生图」按钮，文字内容应使用完整正文区域；节点上方工具条中的生图入口仍可正常创建生成配置。
- 多文本生成：生成次数大于 1 时只创建一个文本结果节点和一条来源连线；节点右上角显示实际生成成功的文本数量，点击后应像多图节点一样展开所有备选文本；点击「设为主文本」应立即替换正文，原主文本保留为备选，双击正文编辑后再次切换回来仍应保留修改。
- 组节点引用：组节点应使用透明、轻量的边框与标题样式，并显示右侧输出连接点；把组连接到生成节点后，组内有内容的图片、视频、音频、文本及可引用扩展节点都应作为输入，普通提示词面板的 `@` 列表应显示组内资源，生成配置的 `@` 列表应允许一次选择整组；组与单节点重复引用时不得重复发送同一资源。
- 多选打组：框选或点选至少两个节点后，应出现包住全部选中节点的圆角虚线框，框上方显示「打组」；点击后用组节点包住这些内容并保持原位置，随后选中该组。选中组后可通过顶部「解散组」、右键菜单或 `Ctrl/Cmd+Shift+G` 取消分组；已同属一组的节点再次多选时应显示「解散组」而不是再打一组。拖入组、组引用和左侧树形列表应继续可用。
- 节点参考栏：普通节点提示词面板、放大编辑器和生成配置输入框上方应显示当前输入节点；图片和视频显示缩略图并可悬停预览，文本、音频及扩展节点显示对应类型；组连接应展开显示组内每个有效内容节点，删除任一展开项都应断开整组连线。点击加号进入画布选择后可连续添加有效参考，已连接、目标自身、已通过组引用的组内节点及不可引用节点不可重复选择，按 ESC 或顶部提示返回输入框；新增连接后 `@` 列表应立即更新。
- 多图透明背景：展开包含透明 PNG 或 WebP 的多图节点时，主图与所有备选图的透明区域都应直接显示画布背景；切换主图前后不得出现白色或主题面板底色。
- 画布选择与平移：画布应默认进入移动模式，左下工具栏应能切换「选择」与「移动」模式并用图标显示当前模式；选择模式应绘制间距清晰的虚线框，虚线长度、间距和粗细在任意缩放比例下保持一致；移动模式应能直接拖动元素，仅在拖动空白背景时平移画布。两种模式下点击画布空白处都应取消选中当前节点，并关闭其顶部工具条与下方提示词面板。按住 Control 或空格应临时反转当前工具且不改变工具栏模式，即选择变移动、移动变范围选择；点击画布页任意按钮后按空格应只临时切换工具，不得再次触发该按钮；输入框和可编辑文本内仍可正常输入空格。中键平移以及 Shift/Cmd 追加选择节点应继续可用。
- 语言路由与国际化：不带语言前缀的英文路由应直接加载，不得出现重定向循环；在 English 与简体中文之间切换后，导航、设置、页面标题、页面描述和 Ant Design 组件文案均应保持所选语言，刷新后仍保留所选语言偏好。
- 全局弹层主题：全站 Dropdown、Menu、Select、Cascader、TreeSelect 等弹层在浅色主题下应使用白色背景、轻灰悬停和选中态，在深色主题下应使用深色背景与低对比度浅色交互态，不应再出现默认黑灰色块、文字对比不足或单个页面配色不一致。
- 画布节点缩放稳定性：导入图片应打开其提示词面板，从任意角立即缩放时面板应暂时隐藏，缩放过程平滑不闪烁、不触发 `Maximum update depth exceeded`，松手后面板恢复；提示词滚动、生成提示词回显、关闭已有图片或文本节点面板后编辑提示词的持久化、放大编辑提示词并实时同步与 `@` 引用、图片编辑、拖放引用和生成配置应继续正常工作。
- 文本生成数量：文本节点下方提示词面板和文本模式下的生成配置节点都应提供独立的 1–15 生成数量，默认为 1，并按该数量创建文本节点；修改文本数量不得影响图片数量，生成配置节点切回图片模式后应继续使用已配置的图片数量。
- 节点编辑入口：图片工具条应默认只显示图标不带文字，且不再包含「编辑」；文本工具条不再包含「编辑」和「编辑文本」。不拖动直接点击图片、文本或视频节点应直接打开其下方提示词面板；图片工具条的文字标签仍可在工具条设置中显式开启。
- 画布左侧元素列表：点击元素整行应平滑定位并选中对应节点，自动缩放不得超过 100%；有内容的图片元素应显示预览按钮，点击后打开大图弹窗且不触发画布定位。
- 画布左侧树形列表：组内节点应缩进显示在所属组下方，并带有清晰的层级连接线；组箭头可以收起和展开子节点，搜索或类型筛选时仍应保留匹配节点所属的组。
- 画布多图生成：开始生成 N 张图片后应立即创建一个包含 N 个可展开槽位的图片节点，并且来源到结果只保留一条连线；生成期间空槽位应独立显示生成中或失败，任何已有图片内容的槽位都必须始终显示图片，任意图片率先成功后应立即成为主图并展示，不等待其他请求完成。收起状态应使用最多三层紧密错开背板的紧凑堆叠，数量控件应清晰可读；只有点击数量控件才向右上方展开或收起槽位，双击图片应打开大图预览。每张已完成的备选图都应保留在两种主题下清晰可读的「设为主图」操作。点击其他位置、拖动、缩放或预览时已展开的图片组应保持展开，且允许多个组同时展开。
- 画布多图主图切换：点击「设为主图」后，节点中心和当前最长边应保持不变，仅按新图片比例调整另一条边；自由变形节点应完全保持现有宽高，画布视口及左下角缩放值不应变化。
- 画布多图失败槽位：包括当前主槽位在内的每个失败槽位都应显示「重试」和「删除」；重试只更新当前槽位，已有成功主图时不得抢占主图；删除只移除当前失败槽位并同步更新图片数量。
- 画布多图尺寸调整：子图展开时拖动根节点尺寸控制点，应保持图片组展开并正常调整尺寸，不应触发 React 嵌套更新或应用错误页。
- 画布多图备选操作：展开后的图片最多按四列排布，批量较大时仍保持紧凑；双击任一已完成的子图应精确预览该图，不改变主图。包括主图在内的每张已完成图片都应提供独立的下载操作，备选图还应保留两种主题下清晰可读的「创建副本」和「设为主图」操作；「创建副本」应在图片组右侧新增一个独立图片节点并选中它，原图片组保持不变。
- Canvas Agent 启动、MCP 状态、模型与权限控制、审批、版本上报、诊断日志、流式回复、历史一致性、多标签页隔离与本地 Skill 管理。
- Canvas Agent 凭据脱敏：Codex 与 Claude CLI 的 stderr 包含 Bearer token、`sk-` 密钥、显式 API key/token/authorization 赋值、ANSI 样式或跨输出分片的凭据时，调试日志和网页诊断事件均不应出现明文；普通 stderr 文本应保持不变。
- 图片 API 模型参数：`gpt-image-*` 的生成和编辑请求不应发送 `response_format`，`dall-e-*` 请求仍应发送 `response_format: "b64_json"`。
- 图片结果 URL 持久化：画布与生图工作台生成时，每个远端 API 结果只下载一次，校验为可解码图片后持久化到 IndexedDB；返回 200 但内容不是图片时应判为失败；下载期间停止画布生成不得保存或应用结果；被 CORS 拦截但可正常显示的图片应保留原始 URL，不产生空 storage key；远端图片可读取时，插件调用仍应收到 Data URL。
- 多参考图编辑：OpenAI 格式在多张参考图时应使用重复的 `image[]` 表单字段提交文件，单张参考图仍使用 `image`；新建的 OpenAI 图片脚本应使用相同字段名。Gemini 渠道和已经保存的自定义脚本不受影响。
- Agent 顶部栏：中英文模式下、不同面板宽度下，标签都应在边缘被裁切前按实际可用宽度隐藏文字并保留小号数量，拖动宽度时不应裁切、换行、横向滚动或错位。
- Agent 消息排版、Markdown 与代码块样式、附件展示、Token 统计、过程时间线、命令折叠分组与滚动跟随行为。
- Agent 输入框快捷引用：在光标处输入 `/` 应可选择已启用的 Skill，输入 `@` 应可选择当前画布素材；引用应作为与正文基线对齐的紧凑标签插入，支持方向键等键盘选择，悬浮或聚焦后可预览内容，发送或刷新后引用位置与样式应保持。
- Agent 消息元数据：发送包含图片附件、Skill 或画布引用的消息后，刷新当前页面、在另一页面恢复同一对话或重启 Canvas Agent，历史消息都应由 Agent 按准确的 `clientMessageId`、`threadId` 和 `turnId` 恢复展示，本地缩略图仍可悬浮或点击查看，不依赖浏览器 IndexedDB 或文本匹配；通信协议版本变化不得清空独立的消息存储，遇到未知版本或损坏清单时应拒绝写入且保留原文件；删除线程后对应缩略图和引用元数据应同时删除，不得按记录数量或文件大小静默裁剪，改动前仅存在于浏览器旧缓存中的消息继续显示普通文本且不尝试迁移。
- 提示词中心布局、来源同步、搜索、插入画布、提示词详情弹窗，以及图像与视频工作台的紧凑分栏提示词选择器。
- 工作台历史图片清理：生图或视频工作台生成内容后，在「我的素材」删除图片、删除画布图片节点、清空画布或删除画布，再次打开对应生成记录时，历史缩略图、结果图片和参考图仍应正常显示；删除生成记录时，其专属本地文件仍应正常清理。
- 生成提示词的文本编号：普通节点或生成配置连接多个文本节点后发起生成，附加在提示词末尾的每段上游文本都应以「【文本1】」「【文本2】」开头并按连接顺序编号，编号与提示词里 `@` 引用及参考内容栏显示的一致；单段文本同样要带编号，图片、视频、音频编号不受影响。
- 局部遮罩编辑：弹窗底部应为「重置」「导出到画布」「立刻生成」；两个按钮都要求已涂抹区域并填写修改要求，「导出到画布」只写入节点和连线不发起请求，结果节点的提示词面板应预填拼好的提示词并可手动生成，「立刻生成」在写入节点后立即生成。涂抹区域并填写修改要求后确认，画布应在原图下方生成一个「遮罩标注」图片节点，内容为原图叠加半透明蓝色标注区域，颜色和透明度应与弹窗里涂抹时看到的一致；结果节点在原图右侧生成，并同时接收来自原图和遮罩标注的两条连线。请求应按普通参考图发送两张图片，提示词按「图片1 / 图片2」编号说明原图和标注区域，不再携带 mask 参数，Gemini 格式渠道也应可用；生成结果只改动标注区域，且不得残留蓝色标注。
- 设置导入导出：导出的配置文件应只包含渠道、模型、生成和提示词来源设置，导入后应恢复这些设置，且任何位置都不应出现 WebDAV 凭据。
- 本地存储设置：设置页应提供「本地存储」页签，只读取 `infinite-canvas` 数据库，展示 IndexedDB 用量、全站用量、浏览器配额与配额百分比，以及各 object store 的记录数和内容大小；本地数据变化后刷新应更新数值，且不阻塞页面其它部分。
- 资产图片下载：我的资产中仍可预览的图片，点击「下载图片」应能保存文件；标题含中文、空格或「【】」时应沿用原标题作为文件名，无需改名；从画布刚存入、尚未刷新页面的图片也应能下载。
- 画布视频任务恢复：开始 OpenAI 风格视频生成后，视频节点应保存远端任务 ID。刷新页面后，尚未完成的视频应自动继续查询状态；若查询已停止，可点击「获取任务状态」继续用该任务 ID 拉取，直到生成成功、任务失败或查询请求报错。任务失败后，「重试」应重新发起生成而不是继续查旧任务。

## 管理后台拆分与 RBAC（本版本新增，待你会话重启后验收）

- **admin 独立站**：打开 `https://sim-admin.youc.online` 应看到登录页（不再是「请先在主站登录」的说明页）。用 `admin@youc.online` / `ad0403485260cc95d9aa` 登录后按 **F5 硬刷新应保持登录**——这一条同时验证了「同站跨源的 SameSite=Lax cookie 被带上」这个关键推断。登录页与侧边栏登出按钮上方应有「登录态与主站共用」的说明。
- **非管理员账号**：用 `test@example.com` / `test123456` 登录 admin 域，应落到「当前账号没有后台访问权限」的说明页，且**不会被登出**（回主站仍是登录态）。
- **按权限隐藏菜单**：在后台「系统 → 角色与成员」建一个只勾 `users.read` 的角色，用一个该角色的账号登录，菜单应只剩用户相关项；直输 `/admin/models` 应落到无权限页，且接口返回 403 而不是重试。
- **角色管理**：建角色、改角色（按模块勾选权限）、删角色；系统角色 `admin` 的权限应不可编辑、不可删除；不能修改自己的角色；不能删除仍有成员的角色。
- **主站入口**：主站用户菜单的后台入口应跳转到 admin 域；`admin.youc.online` 之外，主站 `/admin` 目前仍返回 200（SPA 兜底渲染 NotFound），**真 404 尚未实现**，属已知待办。
- **上游请求对齐**（上一批，仍未在真环境逐项验）：文本对话的 systemPrompt 是否生效；图像尺寸是否按所选分辨率与比例换算成像素；视频的「首尾帧 / 通用参考」是否真的按界面所选发出；视频生成不应再出现 `409 QUOTE_STALE`。
- **数据库注释**：Navicat 中非主键列应带中文注释（239/269）；主键列与表注释仍无，属已知限制。

## 上游请求参数对齐与模型注释（当前待验收）

- 上游请求参数：服务端转发给 AI 上游的请求按原版逐条对齐后，请用真实渠道验证：文本对话的 systemPrompt 是否生效（此前被静默丢弃）；图像生成的尺寸是否按所选分辨率与比例换算成像素；Gemini 渠道的 `imageSize` 在选择高质量时是否为 4K；视频模式的「首尾帧」与「通用参考」是否真的按界面所选发出（此前一律按参考图发出）；带参考视频或参考音频的 Gemini 视频任务是否把它们带上；语音的语速与音色是否按所选生效。
- 视频报价：选择视频模型生成时不应再出现 `409 QUOTE_STALE`；模式切换（首尾帧 / 通用参考）不应影响报价金额。
- 数据库注释：重启 api 后，数据库的非主键列应带上中文注释（可在 Navicat 中查看表结构）；主键列与表本身仍无注释，属已知限制。
- 迁移安全：本次首次启动会对约 239 个非主键列下发 `ALTER TABLE ... MODIFY COLUMN`。请核对 `information_schema.columns` 在升级前后的差异，确认**只有 `COLUMN_COMMENT` 变化**；若列类型或默认值也被改动，说明库内定义与模型不一致，需从备份恢复并单独处理。

## 账号后端与前端登录/会员中心

- 后端部署：测试站已部署在 `https://sim-art.youc.online`（app 绑 `127.0.0.1:3100`，MySQL 额外映射 `127.0.0.1:13306`），代码取自 `codex/account-backend-plan`，配置为 `deploy/env.test`；app / api / db 均已运行，容器内 `/healthz`、`/readyz` 均返回 `{"status":"ok"}`。仍待验收：浏览器登录、媒体上传与播放、SSE 流式输出、容器重启恢复、DBeaver 隧道。测试通过后再按独立正式 env 部署，正式环境的 `JWT_SECRET` 与 `CREDENTIAL_MASTER_KEY` 必须与测试不同。启用免费赠送但未配置活动 ID、风险阈值或每日预算时，api 应拒绝启动并给出明确错误。
- 后端账号链路：注册后应收到验证邮件（`MAIL_DRIVER=log` 时在日志中），验证、登录、刷新、登出全链路正常；并发刷新只有一个请求成功且胜者会话继续有效；连续 5 次登录失败锁定 15 分钟、成功登录后计数清零；同一邮箱发送验证/重置邮件超过 3 次/小时、同一 IP 超过 10 次/小时应返回 429。
- 后端资源与媒体：画布列表/详情/保存正常，保存时 revision 过期返回 409 并附当前 revision；跨用户访问画布、素材、生成记录、媒体均返回 404；未验证邮箱或超过单文件上限时上传被拒；本地上传的媒体可用返回地址直接访问，重复上传同一 storageKey 不产生重复记录。
- 前端登录：未登录访问任意业务页面应重定向到 `/login`，登录成功后回到原路径；刷新已登录页面不闪登录页；注册成功提示验证邮件并可在用户菜单重发；忘记密码与重置密码、`/verify-email` 链接均可完成；`bun run test` 通过。
- 前端会员中心：个人中心（资料、改密、双桶点数与流水、存储用量、注销冷静期）、充值订单、定价、模型广场在浅色/深色与手机宽度下正常，后端接口未就绪时显示加载/空/错误态而非假数据；A 账号登出后 B 账号登录，个人中心与订单不应出现 A 的数据。

## 点数、支付与管理后台（第三期）

- 账本：管理后台向 `granted` 桶加点后只有 `grantedMicros` 增加，流水带正确的桶、金额与写入后余额，档位仍为 `free`；按桶 `SUM(amount_micros)` 始终等于两个桶；把某个桶扣成负数会被拒绝且余额与流水都不变；流水没有任何写接口。
- 档位与配额：只有赠送点数的账号保持 `free`；充值后 `/api/me` 立即显示 `paid` 与更大的存储、单文件与保留期上限，全程无需管理员操作；购买桶耗尽但权益期内仍为 `paid`，到期后先进入 60 天 `sunset` 再落 `free`；用量超过派生档位上限时 `readOnly` 为 true，上传返回 403 `READ_ONLY`（响应带 `planId`），浏览、编辑、删除与导出仍可用；超出档位上限的上传返回 507 `STORAGE_QUOTA_EXCEEDED` 并带 `used` 与 `limit`；并发上传后 `usage.storageBytes` 与 `SUM(media_files.bytes)` 一致。
- 订单与回调：管理员修改档位后已创建订单快照不变；同一 `provider_order_id` 重复回调只到账一次，第二次仍返回渠道要求的成功响应；伪造签名返回 400 `INVALID_SIGNATURE`；金额不一致时拒绝到账并记录日志；取消已支付订单返回 409 `ORDER_ALREADY_PAID`，用他人订单 id 取消返回 404；待支付订单超过 30 分钟先向渠道主动查询、确认后再置失败；连续充值两次时 `paid_until` 从原截止时间继续延长而不会缩短。
- 模型目录与折扣：新增模型后模型广场立即出现且参数表单按 `constraints` 渲染；去掉 `quality` 后不再渲染质量选择器；切换模型时非法已选值自动回落；价格表列出全部组合且每次组合变化都能看到对应价格；保存时拒绝缺组合、重复、负价格与 `constraints` 外的取值；折扣按具体度再按优先级取一条且不叠加；冲突规则返回 409 `PROMOTION_CONFLICT`；停用或修改活动会递增版本，`nextPricingChangeAt` 到点后价格矩阵自动刷新；已产生过流水的模型无法删除只能下架，充值档位没有删除入口。
- 清理与注销：删除画布后对用户执行清理，只删除其独占媒体并降低用量；软删除画布引用的媒体不会被清理；重新编辑画布刷新最后触达后该媒体不再计入过期；预演只列出孤儿与过期项、不删除任何文件，同一次扫描能看到两个结果集；降档清理按最旧优先删到用量回落到免费上限内，不会删光。
- 管理后台：按邮箱、状态与 `planId` 筛选用户，列表展示两个桶、`paidUntil`、档位与存储用量；封禁会撤销刷新令牌，最多 15 分钟完全掉线；重置密码会把该用户全部设备踢下线；非管理员访问任意 `/api/admin/*` 返回 403，访问 `/admin` 路由被重定向到首页；订单可按状态、渠道与用户筛选；模型页用受控表单编辑约束、价格矩阵与折扣活动并可预览折后价格，价格变更都有审计记录。
- 账号注销：申请需要校验密码且重复申请幂等，申请后立即无法生成与充值但仍可浏览与导出；冷静期内撤销后账号恢复；到期后账号被匿名化（占位邮箱与用户名、清空资料与密码、撤销令牌、业务数据软删除）；之后用原邮箱注册得到全新账号。

## 图片分辨率尺寸表

选择分辨率和宽高比后，W/H 按下表填写，单位为像素。预设尺寸不受「16 倍数对齐」开关影响；该开关只控制手动输入尺寸时的对齐。最终生成尺寸以所选模型的实际输出为准。

| 宽高比 | 1K | 2K | 4K |
| --- | ---: | ---: | ---: |
| **1:1** | **1024 × 1024** | **2048 × 2048** | **2880 × 2880** |
| **2:3** | 1024 × 1536 | 1360 × 2048 | 2336 × 3520 |
| **3:2** | 1536 × 1024 | 2048 × 1360 | 3520 × 2336 |
| **4:3** | 1024 × 768 | 2048 × 1536 | 3312 × 2480 |
| **3:4** | 768 × 1024 | 1536 × 2048 | 2480 × 3312 |
| **16:9** | 1536 × 864 | 2048 × 1152 | **3840 × 2160** |
| **9:16** | 864 × 1536 | 1152 × 2048 | **2160 × 3840** |
| **21:9** | 2016 × 864 | 2688 × 1152 | **3840 × 1648** |
| **9:21** | 864 × 2016 | 1152 × 2688 | **1648 × 3840** |

待验收：

- 在画布和图像工作台逐项切换 3 档分辨率与 9 种比例，W/H 应与表格一致，重新打开设置后仍选中正确档位；重点检查 1K 的 2:3、16:9、21:9 及对应竖图，以及 4K 的 1:1。
- 所有 4K 预设提交时均不应触发本地总像素或最长边限制；生成和编辑请求、调用脚本应收到所选像素尺寸。
- 分辨率为自动时保留比例选择，比例为自动时禁用 W/H；手动输入 W/H 后，对齐开关仍应按原有方式工作。
- Gemini 在质量为自动时，应将表中尺寸识别为对应的 1K / 2K / 4K；显式选择质量时维持原有优先级。视频尺寸设置应保持原有行为。

## 画布与素材的资源化改造（第二期前端）

- 画布列表：翻页、关键字搜索与排序都由服务端完成，网络面板里看不到一次性拉取全部画布的请求；列表响应不含 `data`，卡片上「N 个节点 · N 条连线」与实际打开画布后的数量一致。
- 自动保存：编辑画布停手约 2 秒自动保存、顶栏显示「已保存」；持续拖动节点不停手时最多 30 秒也会保存一次；断网后保存失败有提示且可点击重试。
- 断网打开画布：页面停在原处提示重试，不会被当成「画布不存在」跳回列表页。
- 冲突：两台设备打开同一张画布，A 保存后 B 保存会弹出冲突选择框；选「用我的版本覆盖」后 A 刷新看到 B 的内容，选「重新加载」后 B 丢弃本地改动看到 A 的内容；重命名画布不会让另一台设备产生冲突。
- 缩放与输入：缩放节点时不卡顿、松手后只发一次保存；连续输入文本同样只在停手后发一次。
- 素材：关键字（含正文关键词）、类型、标签筛选都命中服务端，标签候选来自列表第一页响应；标签多选为「同时包含」；画布侧栏、素材选择弹窗与 Agent 的 `assets_list` 同样走服务端筛选。
- 生成记录：工作台历史滚动到底自动加载下一页，`nextCursor` 为 null 后停止请求；删掉本地记录后继续翻页不出现空洞；生成历史为空属预期（服务端写入在第四期接入）。
- 视频任务恢复：刷新视频页会发出一次 `GET /api/generations?kind=video&status=pending` 查询用于恢复轮询，与历史分页是两个独立请求。
- 媒体：画布与工作台里的图片、视频、音频均由 `/api/media/{storageKey}` 提供；未验证邮箱的账号上传媒体返回 `EMAIL_NOT_VERIFIED` 但画布本身仍能保存；上传中断后用同一 `storageKey` 重试不会产生重复记录。
- 导出导入：画布导出与素材导出会按 `storageKey` 逐个下载媒体；导入时先上传媒体再建画布/素材，导入后图片不破图。
- 本地数据：「本地存储」面板只显示应用状态、Agent 消息与提示词缓存，浏览器 IndexedDB 里不再保存画布、素材、生成记录与媒体二进制（升级后旧数据不会自动上传，需要保留可先导出 zip 再导入）。

## 测试/正式环境区分与测试数据

- 环境标识：本地开发访问 http://localhost:3000 时，顶栏（含画布编辑页）应显示「本地开发」标识；部署时把 `SITE_ENV` 设成 `test` 显示「测试环境」，设成 `production`（或不设）则不显示任何标识。
- 测试数据导入：在测试环境把 `SEED_TEST_DATA=true` 打开并启动一次服务，应自动写入测试账号 `test@example.com`（密码 `test123456`，邮箱已验证）、一张「示例画布（测试数据）」（2 个文本节点 + 1 条连线）与一条带「测试数据」标签的示例素材；再次启动不应重复写入（可在 Navicat 里核对行数不变）。
- 用测试账号登录后：画布列表能看到示例画布，素材页能看到示例素材且标签筛选可用；正式环境必须保持 `SEED_TEST_DATA=false`。
- 部署脚本：`cp deploy/env.test.example deploy/env.test` 并填写后，先 `./deploy.sh test build` 在本地构建 `infinite-canvas:test` / `infinite-canvas-api:test` 镜像（不再使用上游 ghcr 镜像），再 `./deploy.sh test up -d` 启动；测试环境应只绑定在 `127.0.0.1:3100`（对外端口由 env 的 `APP_PORT` 指定），容器与数据卷带 `-test` 项目前缀，`./deploy.sh prod up -d` 起在 `127.0.0.1:3200`。两个环境的数据库与媒体卷互不可见。测试环境 db 额外映射 `127.0.0.1:13306`，供 Navicat 走 SSH 隧道查看。`up` 固定重建 app 与 api，更新 api 镜像后 `/api` 反代不会指向旧容器 IP。
- 数据库版本：测试与正式 db 容器固定为 `mysql:8.4.11`（8.4 LTS），本地连接同一个测试库、不另起第三个实例；测试首次启动应自动建表并写入样例数据，DBeaver 应可通过 MySQL 驱动与 SSH 隧道连接。
- 测试与正式的 `JWT_SECRET` / `CREDENTIAL_MASTER_KEY` 必须不同：测试令牌不应在正式通过校验；本地 Go 与服务器测试 Go 属于同一逻辑测试环境，完整模式前置条件完成后共用测试密钥。

- 部署文档验收：`deploy/README.md`、Docker 与本地开发页明确两套数据库、SSH/DBeaver 和分阶段实施；本地服务器手册不保存密码。完整双 Go 共享模式仍明确列为 TODO，不应描述为已部署或已验收。本次变更是文档规划，不是服务器部署。

- 用量分析看板：以持有 `stats.usage` 权限的后台角色进入 admin，侧边栏应出现「用量分析」入口且页面可打开；无该权限的角色不应看到入口（页面级守卫与菜单同源）。页面应显示 7/30/90 天切换（页面默认选中 7 天，接口缺省 30）、KPI 卡（生成请求、成功率、消费点数、活跃用户、活跃会话）、按天趋势折线（请求数 + 消费点数）、模型分布条形图（前 10）、能力分布饼图、规格分布条形图与消费用户排行表（前 10），切换天数后图表与 KPI 随之刷新，趋势图缺数日期应补零显示。`days` 传 7/30/90 以外的值接口应返回 400。在主站生成图片、视频、音频和文本对话后，浏览器网络面板中四类请求 body 应带 `sessionId`（同一标签页刷新保持不变，新开标签页为新值）；统计按 UTC+8 日界归天，消费点数只计成功请求（审核拒绝不退点会造成的低估见接口注释）。
