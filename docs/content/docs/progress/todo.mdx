---
title: TODO
description: Planned follow-up work
---

# TODO

- Replace the Claude Code CLI adapter with a Claude Agent SDK adapter and improve the tool queue, following the [Local Agent Integration Plan](/docs/progress/local-agent-integration-plan).
- Add Skill installation from web search, Skill resource-file management, and controlled local-memory persistence.
- Account system and backend services: build the MySQL 8.4 LTS + Go backend per the master plan and phase 1–5 documents under `我的规划/` at the repo root; all features require login and all AI channels are server-configured. Phase 1 (accounts, email, free-grant risk control), phase 2 (resource storage and object storage), phase 3 (double-bucket credits, packages and orders, plan derivation and storage quota, model catalog with promotions, retention cleanup, account deletion, and the admin console), and phase 4 (server-side AI routing with quote, reserve, and failure refund, encrypted platform channels with failover, async video tasks, SSE chat, and the removal of browser-direct channels and scripts) are delivered.
- Phase 4 wrap-up is done: the 429 countdown follows `Retry-After`, a second quote change surfaces a "price updated, generate again" notice instead of resubmitting silently, and video generation records carry the task handle so the page can resume progress after a refresh.
- Phase 5 (content moderation and manual review) is delivered: prompt and reference pre-checks before reserving credits, quarantine-then-publish for artifacts and uploads, NSFWJS / Detoxify adapters, the admin moderation console, and the fail-mode startup gate.
- Phase 5 deployment wrap-up: `MODERATION_PROVIDER=nsfwjs+detoxify` needs the two inference sidecars (Node + Python) deployed on the compose network, and video moderation needs `ffmpeg` in the API container; the test template currently disables moderation; `fake` is available for workflow testing only.
- Compliance gates for public launch stay open and are independent of code: algorithm filing, large-model service filing, legal review of the terms, and confirmed human review staffing must all be completed before public registration and the platform model entry are opened.
- Design-to-API gaps: community, operations activities (daily check-in and invitations), site settings, admin management with audit logs, and the revenue metrics are delivered. Still open: OAuth sign-in, 2FA and login-device management, verification-code sign-in, creation contests, and the docs CMS — each needs a scope decision before its API and pages are built.
- Test-environment deployment: use the rollout in `deploy/README.md`, with only test and production databases; local development shares `infinite_canvas_test`. Compose and image/port isolation are in place. Server checkout, keys/env, build, test-domain Nginx/certificate, SSH/DBeaver access and end-to-end acceptance remain open. Real server details stay in the local-only runbook; production is deployed from an accepted main release tag after backup/restore preparation and domain cutover.
- Admin console deployment is documented in `deploy/README.md` (管理后台): it ships inside the same web app (`/admin/*`) and the same api (`/api/admin/*`), so no extra service, port, domain, or image is needed, and admin APIs are enforced server-side by `Auth` + `AdminOnly`. `ADMIN_EMAIL` / `ADMIN_PASSWORD` only bootstrap the first admin — an existing account is promoted, never re-password-reset — so editing the env file cannot recover a forgotten admin password. Still open: decide whether to restrict admin access before public launch (a proxy access policy covering both `/admin` and `/api/admin/`).
- Scheduled backup for the canvas deployment is still open: after the test environment stabilizes, add a periodic `mysqldump --single-transaction`, a media volume archive, and the env file, restored as a set. Schema upgrades rely on `AutoMigrate`, which has no rollback, so dump production before every upgrade and rehearse the new version on the test environment first.
- Local Go and server test Go sharing one test database: first deploy the same private test S3 bucket with browser-accessible presigned URLs/CORS, then implement controls for schema/seed/admin initialization, startup request convergence and background jobs; the server test Go is the default sole executor. Define setting-cache refresh/restart and schema compatibility. This full mode is not implemented; meanwhile local Vite can use the server test API through SSH without another Go process. Before Docker builds, exclude real deploy env files and the local runbook from the build context; before long-term operation, add ordinary-media cleanup scheduling and disk monitoring.
